// Макет карточки продукта.
//
// 🔒 ЗАЧЕМ ОН ЕСТЬ, ЕСЛИ НИЧЕГО НЕ РИСУЕТ. Он объявляет ГРАНИЦУ СЕГМЕНТА: без
// собственного макета `not-found.tsx` и `error.tsx` этой папки перехватывались бы
// границей раздела выше, и человек, открывший несуществующий товар, попадал бы
// на 404 всего каталога вместо 404 карточки. Граница — и есть содержание файла.
//
// Сессию он не читает: доступ решает макет подгруппы `(staff)`, а данные —
// маршрут API. Третья проверка добавила бы круг и ничего не защитила.
export default function ProductLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
