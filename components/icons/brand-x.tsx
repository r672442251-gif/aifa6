// Знак X (бывший Twitter).
//
// 🔒 ЕДИНСТВЕННОЕ ИСКЛЮЧЕНИЕ ИЗ ПРАВИЛА «ЗНАЧКИ ТОЛЬКО ИЗ lucide-react»
// (владелец 2026-08-14). В lucide знака X нет: там осталась СТАРАЯ ПТИЦА
// (`Twitter`) и «крестик» закрытия (`X`) — первый показывает бренд, которого
// больше не существует, второй означает «закрыть» и в ряду соцсетей читается как
// кнопка выхода. Набор значков не обязан успевать за переименованиями чужих
// компаний, а мы обязаны.
//
// Форма взята из официального знака: две пересекающиеся линии, сплошная заливка.
// `currentColor` — чтобы значок жил по тем же законам темы, что и соседние
// lucide-значки, и не выпадал из ряда при смене темы.
//
// Размер и класс приходят снаружи, как у lucide: подставлять свои значило бы
// сделать из значка исключение ещё и по размеру.
export function BrandX({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="currentColor"
      aria-hidden
      className={className}
      xmlns="http://www.w3.org/2000/svg"
    >
      <path d="M13.6 10.6 20.9 2h-1.7l-6.3 7.5L7.8 2H2l7.7 11.2L2 22h1.7l6.7-8 5.4 8H21.6L13.6 10.6Zm-2.4 2.8-.8-1.1L4.4 3.3h2.7l5 7.2.8 1.1 6.5 9.3h-2.7l-5.3-7.6Z" />
    </svg>
  )
}
