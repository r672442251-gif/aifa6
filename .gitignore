# dependencies
node_modules/

# next.js
.next/
out/

# env
.env
.env.local
.env.*.local

# database (root legacy + data/ folder)
database.sqlite
database.sqlite-shm
database.sqlite-wal
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal

# user files — stay on server, not in git
storage/*
!storage/.gitkeep
data/*
!data/.gitkeep

# knowledge base documents (CRUD-DOCS/) are tracked like any other file and sync with
# git. The earlier "user docs stay server-local / gitignored" rule (step 112) is parked —
# we may revisit keeping them local later.

# site config (Admin -> Site Settings) — live file is per-server user data; the seed
# defaults live in code (config/app-config.defaults.ts), so the dir ships but not the file.
APP-CONFIG/app-config.json

# build
dist/
tsconfig.tsbuildinfo

# os
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*
app/[lang]/**/_list.generated.ts

# swc plugin cache (build artifact)
.swc/

# Превью статических изображений: считаются на сборке (scripts/generate-image-previews.mjs)
lib/images/previews.generated.ts
