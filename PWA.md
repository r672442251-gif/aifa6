# PWA.md — устанавливаемое приложение: что построено и где проходит граница

> Документ корпуса проекта, форма «правило → проверка → пример»: позже он станет навыком.

**Прогрессивное приложение** — это сайт, который телефон ставит к себе как обычное приложение: значок на
экране, запуск без адресной строки, своя заставка. Ни магазинов, ни проверок, ни отдельных сборок под iOS
и Android — тот же сайт.

---

## Закон 1. Манифест — на язык, а не один на сайт

**Правило.** Каждая языковая версия ссылается на свой `/<язык>/manifest.webmanifest`; собирает их общий
строитель `lib/pwa/manifest.ts`. Корневой `/manifest.webmanifest` остаётся для языка по умолчанию.

**Почему.** Установленное приложение подписано на домашнем экране именем из манифеста и открывается с его
`start_url`. Один манифест на весь сайт означал, что испаноязычный посетитель ставит себе приложение с
английским именем, открывающееся на английской главной, — и переименовать значок он уже не сможет. Это то
же самое, что один `<html lang>` на все языки, только на телефоне и навсегда.

**Проверка.** `npm run check:pwa` + `curl /ru/manifest.webmanifest` — `lang`, `name`, `start_url` и ярлыки
на нужном языке.

---

## Закон 2. `id` обязателен

**Правило.** Манифест объявляет `id` (у нас — стартовый адрес языка).

**Почему.** Без `id` система опознаёт установку по `start_url`. Смена стартового адреса — например, при
подключении домена — превращается во ВТОРОЕ приложение рядом с первым вместо обновления существующего, и
у пользователя на экране оказываются два одинаковых значка.

---

## Закон 3. Имя приложения — не заголовок страницы

**Правило.** `name` берётся из имени сайта, а не из `metaForLang().title`.

**Почему.** Заголовок пропущен через шаблон (`%s | Сайт`), и на домашнем экране это выглядит как
«Магазин | Магазин Иванова». Найдено живой проверкой манифеста, а не рассуждением.

---

## Закон 4. Иконки нарезает панель из одного изображения

**Правило.** Владелец загружает одно изображение; набор (192, 512, **maskable**, `apple-touch-icon`,
`favicon` разных размеров, включая `.ico`) генерирует панель.

**Почему maskable отдельно.** Android обрезает значок под форму своей оболочки — круг, скруглённый
квадрат, каплю. Иконка без запаса по краям теряет часть рисунка.

**Стартовый набор есть сразу.** Свежий проект приезжает с нейтральными иконками-заглушками
(`public/icons/`, генератор — `npm run icons:default`): геометрический знак без букв и без чьего-либо
логотипа. Это сделано затем, что манифест без иконок означает ровно одно — приложение НЕЛЬЗЯ
установить, и вся остальная работа по PWA оказывается недоступной пользователю.

Подделывать чужой бренд заглушка не имеет права: пустоту владелец замечает и исправляет, чужой логотип —
нет. Как только он загрузит своё изображение, панель нарежет полный набор в `iconSet`, и тот перекроет
умолчания целиком.

**Проверка.** `npm run check:pwa` требует наличия `maskable` в строителе.

---

## Закон 5. 🔒 Сервис-воркер НИКОГДА не отдаёт страницу из кеша первой

**Правило (`public/sw.js`):**

- **документы (HTML)** — только из сети; кеш используется исключительно при её отсутствии;
- **статика по хешу** (`/_next/static/…`) — из кеша: её адрес меняется с каждой сборкой, устареть она не
  может физически;
- **всё остальное** (api, изображения) — воркер не трогает.

**Почему это главный закон файла.** Кеш страниц — источник самой дорогой жалобы в таких проектах: «я
поменял текст, а в приложении старый». У нас это опаснее вдвойне: панель управления меняет тексты и
настройки БЕЗ пересборки, то есть кешированная страница расходится с настоящей в ту же минуту. При
выбранной стратегии устаревшая страница невозможна по построению.

**Что это даёт и чего не даёт.** Даёт: мгновенное повторное открытие, работу уже виденных страниц без
сети. Не даёт: страницу, которую посетитель ещё не открывал, — офлайн честно покажет ошибку сети.
Отдельной офлайн-страницы нет намеренно: она потребовала бы слов на всех языках, то есть партии перевода.

**Проверка.** `npm run check:pwa` падает на воркере, отдающем документы из кеша (проверено негативным
контролем).

**Где воркера не будет.** В режиме без домена сайт работает по http, а браузер регистрирует воркер только
в защищённом контексте. Приложение при этом полностью работоспособно — теряется только офлайн.

**Страница первого визита кешируется на активации — иначе обещание офлайна ложно.** Воркер рождается
ПОСЛЕ загрузки страницы, ради которой посетитель пришёл: та страница проходит мимо него, и в кеш попадают
только следующие переходы. Посетитель, потерявший сеть на первой же странице, получал ошибку браузера на
том, что видел секунду назад. Поэтому на `activate` воркер спрашивает `clients.matchAll()`, какие страницы
открыты СЕЙЧАС, и сохраняет ровно их. Догадки о будущем здесь нет — страница уже на экране, — поэтому
закону «ничего не предзагружаем» это не противоречит.

Отсюда же второе: ветка офлайна читает кеш ДВАЖДЫ — по объекту запроса и по адресу. Запись могла лечь
навигационным запросом (обычный переход) или строковым ключом (эта самая страница первого визита), а
сопоставление по объекту не обязано находить строковый ключ. Цена промаха — ровно та ошибка браузера,
ради которой всё и написано.

*Найдено руками 2026-08-13: сайт был открыт, DevTools переведён в Offline, обновление дало `ERR_FAILED`.
Симптом читается однозначно — `ERR_FAILED`, а не `ERR_INTERNET_DISCONNECTED`, означает, что воркер
отработал и сам отклонил ответ, не найдя записи.*

---

## Закон 6. Цвет темы — отдельно для светлого и тёмного оформления

**Правило.** `generateViewport` объявляет `themeColor` под обе схемы.

**Почему.** Иначе полоса состояния на телефоне с тёмной темой красится не в тот цвет, и установленное
приложение выглядит чужим.

---

## Закон 7. Предложение установки — своё, и оно молчит, пока браузер не разрешил

**Правило.** Кнопка появляется ТОЛЬКО после события `beforeinstallprompt`. Отказ помнится 30 дней.

**Почему не полагаемся на браузер.** Он умеет предлагать установку сам, но прячет это в меню и в значке
адресной строки — находит тот, кто и так знал, что искать. Посетитель, которому приложение было бы
полезно, о возможности не узнаёт никогда.

**Почему нельзя нарисовать свою кнопку без события.** Окно установки открывает браузер, и только по этому
событию. Кнопка, нарисованная раньше, либо не работает, либо назойлива: браузер присылает событие лишь
когда сайт действительно устанавливаем (манифест с иконками, https, воркер) и посетитель проявил интерес.

**Слова — 82 языка** (`components/pwa/install-prompt.i18n.ts`), набор тот же, что у cookie-баннера. Это
переиспользуемый элемент: он появится в любом языке, который владелец включит, в ту же минуту. Строк
ровно две — чем меньше слов у такого элемента, тем дешевле он переводится и тем реже врёт. Словарь
резолвится на СЕРВЕРЕ и едет пропсом: 82 языка не имеют права оказаться в браузере.

---

## Закон 8. Заставки iOS обязательны, иначе запуск — белый экран

**Правило.** `apple-touch-startup-image` на каждое разрешение, портрет и альбом; список объявлен один раз
в `components/pwa/ios-splash.tsx`, картинки рисует `npm run icons:default`.

**Почему это не мелочь.** Android берёт цвет фона из манифеста и картинки не требует. iOS требует растр
под каждое разрешение и выбирает его медиа-запросом (ширина, высота, плотность, ориентация). Нет
картинки — Safari рисует БЕЛЫЙ ЭКРАН, и на тёмной теме запуск выглядит как вспышка и поломка. Это первое,
что видит человек после установки.

**Проверка.** `npm run check:pwa` сверяет объявленное с файлами на диске: расхождение невидимо — часть
устройств получит ссылку на несуществующую картинку, и заметит это только владелец такого телефона.

---

## Чего сознательно НЕТ

| Не сделано | Почему |
|---|---|
| Офлайн-страница для НЕ виденных страниц | см. закон 5: её слова нужны на всех языках, а показать её можно только там, где всё равно нет данных |

Пункт не «забыт» — он назван, и у него написана причина.

| Команда | Что ловит |
|---|---|
| `npm run check:pwa` | манифест мимо общего строителя; отсутствие языкового манифеста; пропавшие поля и `maskable`; иконку или заставку, объявленную без файла; невключённую регистрацию воркера; **воркер, отдающий страницы из кеша** |
